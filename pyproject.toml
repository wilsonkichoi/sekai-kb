[project]
name = "lagunabeach-md-tools"
version = "0.0.0"
description = "Python tooling for the LagunaBeach.md knowledge base (article-health)."
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = ["pytest>=8"]

[tool.pytest.ini_options]
testpaths = ["tests"]
