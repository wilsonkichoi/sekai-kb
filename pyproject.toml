[project]
name = "sekai-kb-tools"
version = "0.0.0"
description = "Python tooling for a sekai-kb knowledge base (article-health)."
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = ["pytest>=8"]

[tool.pytest.ini_options]
testpaths = ["tests"]
